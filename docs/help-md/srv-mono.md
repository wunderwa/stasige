### yarn srv (stasige ssg) (mono mode)
Start local server
```shell
yarn srv -hcDp 〈port〉
```
* `-c` - clear console
* `-h` - help description
* `-D` - dev mode
* `-p 〈port〉` - port number. Default 8000
